import { makeStyles } from "@mui/styles";

export default makeStyles((theme) => ({
  content: {
    flexGrow: 1,
    marginTop: "80px",
    padding: "20px",
    backgroundClip: "gray",
  },
  root: {
    flexGrow: 1,
  },
}));
